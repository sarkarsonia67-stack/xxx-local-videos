import {auth,db} from "./firebase-config.js";import {createUserWithEmailAndPassword,signInWithEmailAndPassword,updateProfile} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {ref,set} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-database.js";const $=id=>document.getElementById(id);if($("signup"))$("signup").onclick=async()=>{try{const name=$("name").value.trim(),email=$("email").value.trim(),pass=$("password").value;if(!name||!email||pass.length<6)throw Error("Enter valid details. Password must be 6+ characters.");const c=await createUserWithEmailAndPassword(auth,email,pass);await updateProfile(c.user,{displayName:name});await set(ref(db,"users/"+c.user.uid),{userId:c.user.uid,name,email,role:"user",createdAt:Date.now(),status:"active",totalViews:0,totalVideos:0,totalEarning:0,bankStatus:"not_submitted"});location.href="index.html"}catch(e){$("msg").textContent=e.message}};if($("login"))$("login").onclick=async()=>{try{await signInWithEmailAndPassword(auth,$("email").value.trim(),$("password").value);location.href="index.html"}catch(e){$("msg").textContent=e.message}};