import {db} from "./firebase-config.js";import {ref,onValue} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-database.js";const box=document.getElementById("videos");onValue(ref(db,"videos"),s=>{box.innerHTML="";const data=s.val()||{};Object.entries(data).filter(([_,v])=>v.status==="approved"&&v.public===true).sort((a,b)=>(b[1].createdAt||0)-(a[1].createdAt||0)).forEach(([id,v])=>{box.innerHTML+=`<article class="video"><a href="watch.html?id=${id}"><video src="${v.videoUrl}" muted preload="metadata"></video><h3>${esc(v.title)}</h3></a><div class="muted">❤ ${v.likes||0} · 👁 ${v.views||0} · ${esc(v.hashtags||"")}</div></article>`})});function esc(x){return String(x??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]))}