import {auth,db} from "./firebase-config.js";import {onAuthStateChanged} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {ref,onValue} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-database.js";onAuthStateChanged(auth,u=>{if(!u)return location.href="index.html";onValue(ref(db,"users/"+u.uid),a=>{if(a.val()?.role!=="admin")location.href="index.html"});onValue(ref(db,"users"),s=>{list.innerHTML=Object.values(s.val()||{}).map(x=>`<div class="card"><b>${x.name||""}</b><p>30-day views: ${x.last30DaysViews||0}</p><p>Total views: ${x.totalViews||0}</p><p>Earning: ₹${x.totalEarning||0}</p><button onclick="transfer('${x.userId}')">Transfer Earning</button></div>`).join("");window.transfer=id=>alert("Connect payout workflow before transferring real funds for user "+id)})});