import {auth,db} from "./firebase-config.js";import {onAuthStateChanged} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {ref,onValue} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-database.js";onAuthStateChanged(auth,u=>{if(!u)return location.href="login.html";onValue(ref(db,"users/"+u.uid),s=>{const x=s.val()||{};document.getElementById("stats").innerHTML=`<div class="card">Videos: ${x.totalVideos||0}</div><div class="card">Views: ${x.totalViews||0}</div><div class="card">Earning: ₹${x.totalEarning||0}</div><div class="card">Bank: ${x.bankStatus||"not submitted"}</div>`});onValue(ref(db,"videos"),s=>{const box=document.getElementById("myVideos");box.innerHTML="";Object.values(s.val()||{}).filter(v=>v.userId===u.uid).forEach(v=>box.innerHTML+=`<div class="card"><b>${v.title}</b><p>Views ${v.views||0} · Status ${v.status}</p></div>`)})});