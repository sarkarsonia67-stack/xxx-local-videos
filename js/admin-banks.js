import {auth,db} from "./firebase-config.js";import {onAuthStateChanged} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {ref,onValue,update} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-database.js";onAuthStateChanged(auth,u=>{if(!u)return location.href="index.html";onValue(ref(db,"users/"+u.uid),s=>{if(s.val()?.role!=="admin")location.href="index.html"});onValue(ref(db,"bankVerification"),s=>{list.innerHTML=Object.entries(s.val()||{}).filter(([_,x])=>x.status==="pending").map(([id,x])=>`<div class="card"><b>${x.userId}</b><p>${x.bankName||""} · ${x.accountHolder||""}</p><p>IFSC: ${x.ifsc||""}</p><button onclick="verify('${id}','approved')">Approve</button><button onclick="verify('${id}','rejected')">Reject</button></div>`).join("")});window.verify=(id,status)=>update(ref(db,"bankVerification/"+id),{status,processedAt:Date.now()})});